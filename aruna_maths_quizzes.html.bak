<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Lexend:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Aruna's Maths Quizzes</title>
<style>
  * { margin:0; padding:0; box-sizing:border-box; }
  :root {
    --pink:#e91e8c; --purple:#6c3fc5; --teal:#00bcd4;
    --light:#f5f3ff; --dark:#1a1135;
    --correct:#4caf50; --incorrect:#f44336;
  }
  body { font-family:'Lexend','Segoe UI',sans-serif; background:var(--light); color:var(--dark); min-height:100vh; display:flex; flex-direction:column; line-height:1.7; word-spacing:0.05em; font-size:1rem; }

  header { background:linear-gradient(135deg,var(--pink),var(--purple)); color:white; padding:18px 28px; display:flex; align-items:center; justify-content:space-between; box-shadow:0 3px 12px rgba(110,63,197,.3); }
  .header-left { display:flex; align-items:center; gap:12px; }
  .back-btn { background:rgba(255,255,255,.2); border:none; color:white; border-radius:8px; padding:6px 12px; font-size:.85rem; cursor:pointer; text-decoration:none; display:inline-flex; align-items:center; gap:6px; transition:background .2s; }
  .back-btn:hover { background:rgba(255,255,255,.3); }
  header h1 { font-size:1.4rem; font-weight:700; }
  header h1 span { opacity:.8; font-weight:400; font-size:1rem; margin-left:8px; }
  .header-badge { background:rgba(255,255,255,.2); border-radius:20px; padding:6px 14px; font-size:.85rem; font-weight:600; }

  .app { display:flex; flex:1; }
  nav.sidebar { width:260px; min-width:260px; background:var(--dark); color:white; padding:20px 0; overflow-y:auto; }
  .sidebar-title { padding:0 20px 14px; font-size:.75rem; text-transform:uppercase; letter-spacing:1.5px; color:rgba(255,255,255,.4); border-bottom:1px solid rgba(255,255,255,.08); margin-bottom:8px; }
  .topic-btn { display:flex; align-items:center; gap:10px; width:100%; padding:12px 20px; background:none; border:none; color:rgba(255,255,255,.7); text-align:left; cursor:pointer; font-size:.9rem; transition:all .2s; border-left:3px solid transparent; }
  .topic-btn:hover { background:rgba(255,255,255,.06); color:white; }
  .topic-btn.active { background:rgba(233,30,140,.18); color:white; border-left-color:var(--pink); font-weight:600; }
  .topic-icon { font-size:1.1rem; width:24px; text-align:center; }
  .topic-count { margin-left:auto; background:rgba(255,255,255,.1); border-radius:10px; padding:2px 8px; font-size:.75rem; }

  main { flex:1; padding:28px 32px; overflow-y:auto; display:flex; flex-direction:column; }
  .screen { display:none; }
  .screen.active { display:block; }

  .home-hero { position:relative; overflow:hidden; background:linear-gradient(135deg,#e91e8c,#6c3fc5); border-radius:20px; padding:40px 36px; margin-bottom:28px; color:white; }
  .home-hero::before { content:'➗'; position:absolute; right:28px; top:10px; font-size:6rem; opacity:.12; line-height:1; pointer-events:none; }
  .home-hero h2 { font-size:1.75rem; font-weight:800; margin-bottom:8px; }
  .home-hero p { opacity:.88; font-size:1rem; max-width:520px; line-height:1.6; }
  .home-stats { display:flex; gap:10px; margin-top:16px; flex-wrap:wrap; }
  .home-stat { background:rgba(255,255,255,.18); border-radius:10px; padding:6px 14px; font-size:.82rem; font-weight:600; }

  .topic-cards { display:grid; grid-template-columns:1fr 1fr; gap:14px; margin-bottom:20px; }
  .topic-card { background:white; border-radius:14px; padding:20px 22px; cursor:pointer; transition:all .2s; border:1.5px solid #e0daf5; box-shadow:0 2px 10px rgba(110,63,197,.08); }
  .topic-card:hover { transform:translateY(-2px); box-shadow:0 6px 20px rgba(110,63,197,.14); border-color:var(--pink); }
  .topic-card-icon { font-size:1.6rem; margin-bottom:8px; }
  .topic-card-title { font-size:1rem; font-weight:700; color:var(--dark); margin-bottom:4px; }
  .topic-card-meta { font-size:.8rem; color:#666; }
  .topic-card-meta span { color:var(--purple); font-weight:600; }

  .test-card { background:linear-gradient(135deg,#6c3fc5,#3a1f6e); border-radius:14px; padding:20px 22px; cursor:pointer; transition:all .2s; color:white; box-shadow:0 4px 16px rgba(110,63,197,.3); }
  .test-card:hover { transform:translateY(-2px); box-shadow:0 8px 28px rgba(110,63,197,.4); }
  .test-card-icon { font-size:1.6rem; margin-bottom:8px; }
  .test-card-title { font-size:1rem; font-weight:700; margin-bottom:4px; }
  .test-card-meta { font-size:.8rem; opacity:.8; }
  .test-card-meta span { font-weight:600; opacity:1; }

  .quiz-topic-header { margin-bottom:20px; }
  .quiz-topic-header h2 { font-size:1.5rem; font-weight:700; color:var(--purple); margin-bottom:4px; }
  .quiz-topic-header p { color:#555; font-size:.9rem; }

  .progress-bar-bg { background:#e0daf5; border-radius:4px; height:8px; margin-bottom:18px; overflow:hidden; }
  .progress-bar-fill { height:100%; background:linear-gradient(90deg,var(--pink),var(--purple)); border-radius:4px; transition:width .4s; }

  .question-box { background:white; border-radius:14px; padding:24px 26px; margin-bottom:20px; box-shadow:0 2px 12px rgba(110,63,197,.1); border:1.5px solid #e0daf5; }
  .question-counter { font-size:.78rem; font-weight:700; color:var(--teal); text-transform:uppercase; letter-spacing:1px; margin-bottom:10px; }
  .question-text { font-size:1.1rem; font-weight:600; line-height:1.55; color:var(--dark); }

  .options { display:flex; flex-direction:column; gap:8px; margin-bottom:16px; }
  .option-btn { display:flex; align-items:center; gap:12px; padding:12px 16px; border-radius:10px; border:1.5px solid #e0daf5; background:white; text-align:left; cursor:pointer; font-size:.92rem; transition:all .18s; color:var(--dark); }
  .option-btn:hover:not(:disabled) { border-color:var(--purple); background:#f5f3ff; }
  .option-btn.selected { border-color:var(--pink); background:#fde7f3; }
  .option-btn.correct-ans { border-color:var(--correct); background:#e8f5e9; color:#1b5e20; }
  .option-btn.wrong-ans { border-color:var(--incorrect); background:#ffebee; color:#b71c1c; }
  .option-btn:disabled { cursor:default; }
  .opt-letter { font-weight:700; color:var(--purple); width:20px; text-align:center; flex-shrink:0; }

  .tf-row { display:flex; gap:12px; margin-bottom:16px; }
  .tf-btn { flex:1; padding:14px; border-radius:10px; border:1.5px solid #e0daf5; background:white; font-size:1rem; font-weight:700; cursor:pointer; transition:all .18s; }
  .tf-btn.true-btn:hover:not(:disabled) { border-color:var(--correct); background:#e8f5e9; color:#1b5e20; }
  .tf-btn.false-btn:hover:not(:disabled) { border-color:var(--incorrect); background:#ffebee; color:#b71c1c; }
  .tf-btn.correct-ans { border-color:var(--correct); background:#e8f5e9; color:#1b5e20; }
  .tf-btn.wrong-ans { border-color:var(--incorrect); background:#ffebee; color:#b71c1c; }
  .tf-btn:disabled { cursor:default; }

  .feedback-box { display:none; padding:14px 16px; border-radius:10px; margin-bottom:16px; font-size:.9rem; line-height:1.6; }
  .feedback-box.show { display:block; }
  .feedback-box.correct { background:#e8f5e9; border-left:4px solid var(--correct); color:#1b5e20; }
  .feedback-box.incorrect { background:#ffebee; border-left:4px solid var(--incorrect); color:#b71c1c; }
  .feedback-box .fb-title { font-weight:700; margin-bottom:4px; }
  .feedback-box .fb-explain { color:#333; margin-top:6px; font-size:.88rem; }

  .quiz-nav { display:flex; gap:10px; }
  .nav-btn { padding:10px 22px; border-radius:10px; border:1.5px solid #e0daf5; background:white; color:var(--dark); font-size:.9rem; font-weight:600; cursor:pointer; transition:all .18s; }
  .nav-btn:hover { background:#f5f3ff; border-color:var(--purple); }
  .nav-btn.primary { background:linear-gradient(135deg,var(--pink),var(--purple)); color:white; border-color:transparent; }
  .nav-btn.primary:hover { opacity:.9; }

  .result-hero { text-align:center; background:linear-gradient(135deg,#fde7f3,#ede7f6); border-radius:20px; padding:40px; margin-bottom:24px; border:2px solid var(--pink); }
  .result-emoji { font-size:4rem; margin-bottom:12px; }
  .result-score-big { font-size:3.5rem; font-weight:800; margin-bottom:6px; background:linear-gradient(135deg,var(--pink),var(--purple)); -webkit-background-clip:text; -webkit-text-fill-color:transparent; background-clip:text; }
  .result-label { font-size:1rem; color:#555; margin-bottom:10px; }
  .result-message { font-size:1.1rem; font-weight:600; color:var(--dark); }
  .result-actions { display:flex; gap:12px; justify-content:center; margin-top:20px; flex-wrap:wrap; }
  .result-btn { padding:11px 24px; border-radius:10px; font-size:.9rem; font-weight:600; cursor:pointer; transition:all .18s; border:1.5px solid; }
  .result-btn.primary { background:linear-gradient(135deg,var(--pink),var(--purple)); color:white; border-color:transparent; }
  .result-btn.secondary { background:white; color:var(--purple); border-color:#e0daf5; }
  .result-btn:hover { transform:translateY(-1px); }

  #quiz-retry-nudge { margin-top:10px; font-size:.88rem; color:var(--incorrect); font-weight:600; text-align:center; display:none; }
  #quiz-pass-actions { display:none; text-align:center; margin-top:8px; font-size:.88rem; color:var(--correct); font-weight:600; }

  .review-list { list-style:none; display:flex; flex-direction:column; gap:10px; }
  .review-item { padding:14px 16px; border-radius:10px; border:1.5px solid; }
  .review-item.correct { border-color:#c8e6c9; background:#f1f8f1; }
  .review-item.incorrect { border-color:#ffcdd2; background:#fff5f5; }
  .review-q { font-weight:600; font-size:.9rem; margin-bottom:8px; }
  .review-answer { display:flex; gap:16px; font-size:.82rem; }
  .review-answer .your { color:#555; }
  .review-answer .your span { color:var(--incorrect); font-weight:600; }
  .review-answer .model { color:#555; }
  .review-answer .model span { color:var(--correct); font-weight:600; }

  .test-header { margin-bottom:20px; }
  .test-header h2 { font-size:1.5rem; font-weight:700; color:var(--purple); margin-bottom:4px; }
  .test-header p { color:#555; font-size:.9rem; }
  .test-question-block { background:white; border-radius:12px; padding:20px 22px; margin-bottom:14px; border:1.5px solid #e0daf5; box-shadow:0 2px 8px rgba(110,63,197,.07); }
  .test-q-num { font-size:.75rem; text-transform:uppercase; font-weight:700; color:var(--teal); letter-spacing:1px; margin-bottom:8px; }
  .test-q-text { font-size:1rem; font-weight:600; margin-bottom:14px; line-height:1.5; }
  .test-options { display:flex; flex-direction:column; gap:7px; }
  .test-option { display:flex; align-items:center; gap:10px; padding:10px 14px; border-radius:9px; border:1.5px solid #e0daf5; background:white; cursor:pointer; font-size:.88rem; transition:all .15s; }
  .test-option:hover:not(.disabled) { border-color:var(--purple); background:#f5f3ff; }
  .test-option.selected { border-color:var(--pink); background:#fde7f3; }
  .test-option.correct-ans { border-color:var(--correct); background:#e8f5e9; }
  .test-option.wrong-ans { border-color:var(--incorrect); background:#ffebee; }
  .test-option.disabled { cursor:default; }
  .test-opt-letter { font-weight:700; color:var(--purple); width:18px; }
  .test-tf-row { display:flex; gap:10px; }
  .test-tf-btn { flex:1; padding:10px; border-radius:9px; border:1.5px solid #e0daf5; background:white; font-weight:700; cursor:pointer; font-size:.9rem; transition:all .15s; }
  .test-tf-btn.selected { border-color:var(--pink); background:#fde7f3; }
  .test-tf-btn.correct-ans { border-color:var(--correct); background:#e8f5e9; }
  .test-tf-btn.wrong-ans { border-color:var(--incorrect); background:#ffebee; }

  .submit-btn { display:block; width:100%; padding:16px; border-radius:12px; border:none; background:linear-gradient(135deg,var(--pink),var(--purple)); color:white; font-size:1.1rem; font-weight:700; cursor:pointer; transition:all .2s; margin-top:16px; }
  .submit-btn:hover { opacity:.9; transform:translateY(-1px); }

  .test-result-hero { text-align:center; background:linear-gradient(135deg,#fde7f3,#ede7f6); border-radius:20px; padding:40px; margin-bottom:24px; border:2px solid var(--pink); }
  .test-score-grid { display:grid; grid-template-columns:repeat(3,1fr); gap:12px; margin-top:24px; }
  .test-score-cell { background:white; border-radius:12px; padding:14px; text-align:center; box-shadow:0 2px 8px rgba(110,63,197,.1); }
  .test-score-cell .num { font-size:2rem; font-weight:800; background:linear-gradient(135deg,var(--pink),var(--purple)); -webkit-background-clip:text; -webkit-text-fill-color:transparent; background-clip:text; }
  .test-score-cell .lbl { font-size:.78rem; color:#777; text-transform:uppercase; letter-spacing:.5px; }
  .test-result-actions { display:flex; gap:12px; justify-content:center; margin-top:24px; flex-wrap:wrap; }

  /* ── A11Y TOOLBAR ── */
  #a11y-toolbar{position:fixed!important;bottom:24px!important;right:24px!important;z-index:2147483647!important;font-family:'Lexend','Segoe UI',sans-serif;}
  #a11y-fab{width:50px;height:50px;border-radius:50%;background:#6c3fc5;border:none;color:#fff;font-size:1.3rem;cursor:pointer;box-shadow:0 4px 18px rgba(0,0,0,.35);display:flex;align-items:center;justify-content:center;transition:transform .2s,background .2s;}
  #a11y-fab:hover{background:#4a2a8a;transform:scale(1.07);}
  #a11y-fab.open{background:#4a2a8a;}
  #a11y-panel{position:absolute;bottom:62px;right:0;background:#fff;border-radius:16px;box-shadow:0 8px 32px rgba(0,0,0,.2);padding:16px;width:260px;border:1px solid rgba(0,0,0,.07);}
  #a11y-panel[hidden]{display:none!important;}
  .a11y-title{font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:1px;color:#888;margin-bottom:12px;}
  .a11y-action-btn{display:flex;align-items:center;gap:10px;width:100%;padding:10px 12px;border-radius:10px;background:#f5f3ff;border:1.5px solid #e0daf5;color:#6c3fc5;font-size:.9rem;font-weight:600;cursor:pointer;font-family:inherit;}
  .a11y-action-btn.a11y-stop{background:#fee2e2;border-color:#fca5a5;color:#dc2626;}
  .a11y-divider{height:1px;background:#e5e7eb;margin:12px 0;}
  .a11y-row{display:flex;align-items:center;justify-content:space-between;margin-bottom:10px;}
  .a11y-col-row{flex-direction:column;align-items:flex-start;gap:8px;margin-bottom:4px;}
  .a11y-label{font-size:.85rem;color:#222;font-weight:500;}
  .a11y-pill{padding:4px 14px;border-radius:20px;border:2px solid #d1d5db;background:#f3f4f6;color:#6b7280;font-size:.78rem;font-weight:700;cursor:pointer;font-family:inherit;}
  .a11y-pill.on{background:#6c3fc5;border-color:#6c3fc5;color:#fff;}
  #a11y-colours{display:flex;gap:7px;flex-wrap:wrap;}
  .a11y-col-chip{width:28px;height:28px;border-radius:50%;border:2.5px solid #d1d5db;cursor:pointer;font-size:.65rem;display:flex;align-items:center;justify-content:center;background:#e5e7eb;}
  .a11y-col-chip.sel{border-color:#6c3fc5;border-width:3px;}
  #a11y-overlay{position:fixed!important;inset:0;pointer-events:none;z-index:2147483646!important;transition:background .35s;}
  b.bionic-b{font-weight:800;}
  .a11y-stepper{display:flex;gap:4px;}
  .a11y-step-btn{width:32px;height:28px;border-radius:8px;border:2px solid #d1d5db;background:#f3f4f6;color:#6b7280;font-size:.82rem;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;font-family:inherit;transition:background .15s,border-color .15s;}
  .a11y-step-btn.on{background:#6c3fc5;border-color:#6c3fc5;color:#fff;}
  #a11y-ruler-line{position:fixed!important;left:0;right:0;height:34px;pointer-events:none;z-index:2147483645!important;background:rgba(108,63,197,.1);border-top:2px solid rgba(108,63,197,.35);border-bottom:2px solid rgba(108,63,197,.35);display:none;top:0;}
  body.ruler-on #a11y-ruler-line{display:block;}
  body.opendyslexic,body.opendyslexic *{font-family:'OpenDyslexic','Lexend','Segoe UI',sans-serif!important;}

</style>
</head>
<body>
<div id="a11y-toolbar"><button id="a11y-fab" title="Accessibility tools" aria-label="Accessibility tools">&#9681;</button><div id="a11y-panel" hidden><div class="a11y-title">Accessibility</div><button id="a11y-tts" class="a11y-action-btn"><span id="a11y-tts-icon">&#128266;</span><span id="a11y-tts-label"> Read aloud</span></button><div class="a11y-divider"></div><div class="a11y-row"><span class="a11y-label">Bionic reading</span><button id="a11y-bionic" class="a11y-pill">Off</button></div><div class="a11y-row"><span class="a11y-label">OpenDyslexic font</span><button id="a11y-od" class="a11y-pill">Off</button></div><div class="a11y-row"><span class="a11y-label">Reading ruler</span><button id="a11y-ruler-btn" class="a11y-pill">Off</button></div><div class="a11y-divider"></div><div class="a11y-row"><span class="a11y-label">Text size</span><div class="a11y-stepper"><button class="a11y-step-btn on" id="a11y-sz1" title="Normal">A</button><button class="a11y-step-btn" id="a11y-sz2" title="Large">A+</button><button class="a11y-step-btn" id="a11y-sz3" title="XL">A++</button></div></div><div class="a11y-row"><span class="a11y-label">Line spacing</span><div class="a11y-stepper"><button class="a11y-step-btn on" id="a11y-ls1" title="Normal">1&#215;</button><button class="a11y-step-btn" id="a11y-ls2" title="Relaxed">1.5&#215;</button><button class="a11y-step-btn" id="a11y-ls3" title="Wide">2&#215;</button></div></div><div class="a11y-divider"></div><div class="a11y-row a11y-col-row"><span class="a11y-label">Colour overlay</span><div id="a11y-colours"><button class="a11y-col-chip sel" data-col="none" title="None">&#x2715;</button><button class="a11y-col-chip" data-col="cream" style="background:#fdf6e3" title="Cream"></button><button class="a11y-col-chip" data-col="yellow" style="background:#fff9c4" title="Yellow"></button><button class="a11y-col-chip" data-col="blue" style="background:#dbeafe" title="Blue"></button><button class="a11y-col-chip" data-col="green" style="background:#dcfce7" title="Green"></button><button class="a11y-col-chip" data-col="peach" style="background:#fce7f3" title="Peach"></button></div></div></div></div><div id="a11y-overlay"></div><div id="a11y-ruler-line"></div>

<header>
  <div class="header-left">
    <a class="back-btn" href="index.html">← Back</a>
    <h1>📝 Aruna's Quizzes &amp; Tests <span>Maths · GCSE Year 10</span></h1>
  </div>
  <div class="header-badge">Year 10</div>
</header>

<div class="app">
  <nav class="sidebar">
    <div class="sidebar-title">Topics</div>
    <button class="topic-btn" onclick="showHome()" id="sideHome">
      <span class="topic-icon">🏠</span> Home
    </button>
    <button class="topic-btn" onclick="startQuiz(0)" id="topicBtn0">
      <span class="topic-icon">🔢</span> Number &amp; Algebra
      <span class="topic-count">20</span>
    </button>
    <button class="topic-btn" onclick="startQuiz(1)" id="topicBtn1">
      <span class="topic-icon">⚖️</span> Ratio, Proportion &amp; Rates
      <span class="topic-count">20</span>
    </button>
    <button class="topic-btn" onclick="startQuiz(2)" id="topicBtn2">
      <span class="topic-icon">📐</span> Geometry &amp; Measures
      <span class="topic-count">20</span>
    </button>
    <button class="topic-btn" onclick="startQuiz(3)" id="topicBtn3">
      <span class="topic-icon">📊</span> Statistics &amp; Probability
      <span class="topic-count">20</span>
    </button>
    <button class="topic-btn" onclick="startQuiz(4)" id="topicBtn4">
      <span class="topic-icon">📈</span> Graphs &amp; Functions
      <span class="topic-count">20</span>
    </button>
    <button class="topic-btn" onclick="startTest()" id="topicBtnTest" style="margin-top:8px;border-top:1px solid rgba(255,255,255,.1);padding-top:16px;">
      <span class="topic-icon">🏆</span> Full Test
      <span class="topic-count">50</span>
    </button>
  </nav>

  <main>

    <!-- HOME -->
    <div class="screen active" id="screenHome">
      <div class="home-hero">
        <h2>📝 Maths Quizzes &amp; Tests</h2>
        <p>Practice with quizzes for each topic, then tackle the full 50-question test. You need 80% to pass — go for it, Aruna!</p>
        <div class="home-stats">
          <div class="home-stat">📚 5 Topics</div>
          <div class="home-stat">❓ 100 Quiz Questions</div>
          <div class="home-stat">🏆 50-Question Test</div>
        </div>
      </div>
      <div class="topic-cards">
        <div class="topic-card" onclick="startQuiz(0)">
          <div class="topic-card-icon">🔢</div>
          <div class="topic-card-title">Number &amp; Algebra</div>
          <div class="topic-card-meta"><span>20 questions</span> · Indices, surds, equations, expanding &amp; factorising</div>
        </div>
        <div class="topic-card" onclick="startQuiz(1)">
          <div class="topic-card-icon">⚖️</div>
          <div class="topic-card-title">Ratio, Proportion &amp; Rates</div>
          <div class="topic-card-meta"><span>20 questions</span> · Ratio, percentages, compound measures, growth &amp; decay</div>
        </div>
        <div class="topic-card" onclick="startQuiz(2)">
          <div class="topic-card-icon">📐</div>
          <div class="topic-card-title">Geometry &amp; Measures</div>
          <div class="topic-card-meta"><span>20 questions</span> · Angles, circle theorems, Pythagoras, trigonometry</div>
        </div>
        <div class="topic-card" onclick="startQuiz(3)">
          <div class="topic-card-icon">📊</div>
          <div class="topic-card-title">Statistics &amp; Probability</div>
          <div class="topic-card-meta"><span>20 questions</span> · Averages, cumulative frequency, probability trees</div>
        </div>
        <div class="topic-card" onclick="startQuiz(4)">
          <div class="topic-card-icon">📈</div>
          <div class="topic-card-title">Graphs &amp; Functions</div>
          <div class="topic-card-meta"><span>20 questions</span> · Linear, quadratic graphs, gradients &amp; transformations</div>
        </div>
        <div class="test-card" onclick="startTest()">
          <div class="test-card-icon">🏆</div>
          <div class="test-card-title">Full Maths Test</div>
          <div class="test-card-meta"><span>50 questions</span> · All topics · 80% to pass</div>
        </div>
      </div>
    </div>

    <!-- QUIZ -->
    <div class="screen" id="screenQuiz">
      <div class="quiz-topic-header">
        <h2 id="quizTopicTitle"></h2>
        <p id="quizTopicDesc"></p>
      </div>
      <div class="progress-bar-bg">
        <div class="progress-bar-fill" id="quizProgressFill" style="width:0%"></div>
      </div>
      <div class="question-box">
        <div class="question-counter" id="questionCounter">Question 1 of 20</div>
        <div class="question-text" id="questionText"></div>
      </div>
      <div id="answerArea"></div>
      <div class="feedback-box" id="feedbackBox">
        <div class="fb-title" id="fbTitle"></div>
        <div class="fb-explain" id="fbExplain"></div>
      </div>
      <div class="quiz-nav">
        <button class="nav-btn" onclick="showHome()">← Home</button>
        <button class="nav-btn primary" id="nextBtn" onclick="nextQuestion()" style="display:none">Next →</button>
      </div>
    </div>

    <!-- QUIZ RESULT -->
    <div class="screen" id="screenResult">
      <div class="result-hero">
        <div class="result-emoji" id="resultEmoji">🎉</div>
        <div class="result-score-big" id="resultScore">0%</div>
        <div class="result-label" id="resultLabel">0 out of 20 correct</div>
        <div class="result-message" id="resultMessage"></div>
      </div>
      <p id="quiz-retry-nudge">Looks like this one needs another go.</p>
      <p id="quiz-pass-actions">Well done — you can move on to the next topic! 🌟</p>
      <div class="result-actions">
        <button class="result-btn primary" onclick="retryQuiz()">🔁 Try Again</button>
        <button class="result-btn secondary" onclick="showReview()">📋 Review Answers</button>
        <button class="result-btn secondary" onclick="showHome()">🏠 Home</button>
      </div>
      <div id="reviewSection" style="display:none;margin-top:28px;">
        <h3 style="margin-bottom:14px;font-size:1.1rem;color:var(--purple);">Answer Review</h3>
        <ul class="review-list" id="reviewList"></ul>
      </div>
    </div>

    <!-- TEST -->
    <div class="screen" id="screenTest">
      <div class="test-header">
        <h2>🏆 Full Maths Test</h2>
        <p>50 questions across all topics — complete all questions then submit. You need 80% (40/50) to pass.</p>
      </div>
      <div id="test-body"></div>
      <button class="submit-btn" id="submitBtn" onclick="submitTest()">Submit Test ✓</button>
    </div>

    <!-- TEST RESULT -->
    <div class="screen" id="screenTestResult">
      <div class="test-result-hero">
        <div class="result-emoji" id="testResultEmoji">🏆</div>
        <div class="result-score-big" id="testResultScore">0%</div>
        <div style="font-size:1.1rem;font-weight:600;margin-top:4px;" id="testResultMsg"></div>
        <div class="test-score-grid">
          <div class="test-score-cell"><div class="num" id="tcCorrect">0</div><div class="lbl">Correct</div></div>
          <div class="test-score-cell"><div class="num" id="tcIncorrect">0</div><div class="lbl">Wrong</div></div>
          <div class="test-score-cell"><div class="num" id="tcPercent">0%</div><div class="lbl">Score</div></div>
        </div>
      </div>
      <p id="test-retry-nudge" style="display:none;text-align:center;color:var(--incorrect);font-weight:600;font-size:.9rem;margin-bottom:16px;">Looks like this one needs another go.</p>
      <div class="test-result-actions">
        <button class="result-btn primary" onclick="startTest()">🔁 Retry Test</button>
        <button class="result-btn secondary" onclick="showTestReview()">📋 Review Answers</button>
        <button class="result-btn secondary" onclick="showHome()">🏠 Home</button>
      </div>
      <div id="testReviewSection" style="display:none;margin-top:28px;">
        <h3 style="margin-bottom:14px;font-size:1.1rem;color:var(--purple);">Test Answer Review</h3>
        <ul class="review-list" id="testReviewList"></ul>
      </div>
    </div>

  </main>
</div>

<script>
(function(){
var S='ateam_a11y',st={bionic:false,overlay:'none',od:false,ruler:false,sz:1,ls:1};
try{Object.assign(st,JSON.parse(localStorage.getItem(S)||'{}'));}catch(e){}
var fab,panel,ttsBtn,ttsIcon,ttsLbl,bioBtn,ovl,ttsOn=false;
function init(){
  fab=document.getElementById('a11y-fab');
  panel=document.getElementById('a11y-panel');
  ttsBtn=document.getElementById('a11y-tts');
  ttsIcon=document.getElementById('a11y-tts-icon');
  ttsLbl=document.getElementById('a11y-tts-label');
  bioBtn=document.getElementById('a11y-bionic');
  ovl=document.getElementById('a11y-overlay');
  if(!fab)return;
  var OV={none:'transparent',cream:'rgba(253,246,227,.5)',yellow:'rgba(255,249,196,.5)',blue:'rgba(219,234,254,.5)',green:'rgba(220,252,231,.5)',peach:'rgba(252,231,243,.5)'};
  fab.addEventListener('click',function(e){e.stopPropagation();var o=panel.hasAttribute('hidden');if(o)panel.removeAttribute('hidden');else panel.setAttribute('hidden','');fab.classList.toggle('open',o);});
  document.addEventListener('click',function(e){var tb=document.getElementById('a11y-toolbar');if(tb&&!tb.contains(e.target))panel.setAttribute('hidden','');});
  var ttsOn=false;
  function getText(){var fc=document.getElementById('flashcard');if(fc){var id=fc.classList.contains('flipped')?'cardAnswer':'cardQuestion';var el=document.getElementById(id);if(el&&el.innerText.trim())return el.innerText.trim();}var q=document.querySelector('.screen.active .q-text');if(q)return q.innerText.trim();var ns=document.querySelectorAll('.notes-section.open .notes-section-body');if(ns.length)return Array.from(ns).map(function(n){return n.innerText.trim();}).join('\n\n');var m=document.querySelector('main')||document.querySelector('.screen.active');return m?m.innerText.trim().slice(0,2500):'';}
  function startTTS(){window.speechSynthesis.cancel();var t=getText();if(!t)return;var u=new SpeechSynthesisUtterance(t);u.rate=0.88;u.onend=u.onerror=function(){setTS(false);};window.speechSynthesis.speak(u);setTS(true);}
  function stopTTS(){window.speechSynthesis.cancel();setTS(false);}
  function setTS(on){ttsOn=on;ttsBtn.classList.toggle('a11y-stop',on);ttsIcon.innerHTML=on?'&#9209;':'&#128266;';ttsLbl.textContent=on?' Stop':' Read aloud';}
  ttsBtn.addEventListener('click',function(){ttsOn?stopTTS():startTTS();});
  function bioW(w){if(w.length<=2)return w;var h=Math.ceil(w.length/2);return '<b class="bionic-b">'+w.slice(0,h)+'</b>'+w.slice(h);}
  function walkBold(node){if(node.nodeType===3){var t=node.textContent;if(!t.trim())return;var html=t.replace(/([A-Za-zÀ-ɏ]{2,})/g,function(w){return bioW(w);});if(html===t)return;var s=document.createElement('span');s.innerHTML=html;node.parentNode.replaceChild(s,node);}else if(node.nodeType===1&&!/^(SCRIPT|STYLE|B|BUTTON|INPUT)$/.test(node.tagName))Array.from(node.childNodes).forEach(walkBold);}
  var TG='#cardQuestion,#cardAnswer,.q-text,.option-btn,.notes-section-body,.card-text,.home-hero p';
  function applyBio(){document.querySelectorAll(TG).forEach(function(el){if(el.dataset.bd)return;el.dataset.bd='1';el.dataset.bo=el.innerHTML;walkBold(el);});}
  function removeBio(){document.querySelectorAll('[data-bd]').forEach(function(el){el.innerHTML=el.dataset.bo||el.innerHTML;delete el.dataset.bd;delete el.dataset.bo;});}
  ['flipCard','loadQuestion','showQuestion','nextCard','prevCard'].forEach(function(fn){if(typeof window[fn]==='function'){var o=window[fn];window[fn]=function(){var r=o.apply(this,arguments);if(st.bionic)setTimeout(applyBio,80);return r;};}});
  var bt;new MutationObserver(function(){if(!st.bionic)return;clearTimeout(bt);bt=setTimeout(applyBio,80);}).observe(document.body,{childList:true,subtree:true});
  function setBio(on){st.bionic=on;bioBtn.textContent=on?'On':'Off';bioBtn.classList.toggle('on',on);if(on)applyBio();else removeBio();save();}
  bioBtn.addEventListener('click',function(){setBio(!st.bionic);});
  function setOvl(col){st.overlay=col;ovl.style.background=OV[col]||'transparent';document.querySelectorAll('.a11y-col-chip').forEach(function(c){c.classList.toggle('sel',c.dataset.col===col);});save();}
  document.querySelectorAll('.a11y-col-chip').forEach(function(btn){btn.addEventListener('click',function(){setOvl(btn.dataset.col);});});
  var odBtn=document.getElementById('a11y-od');
  function setOD(on){st.od=on;document.body.classList.toggle('opendyslexic',on);if(on&&!document.getElementById('od-font')){var l=document.createElement('link');l.id='od-font';l.rel='stylesheet';l.href='https://fonts.cdnfonts.com/css/opendyslexic';document.head.appendChild(l);}if(odBtn){odBtn.textContent=on?'On':'Off';odBtn.classList.toggle('on',on);}save();}
  if(odBtn)odBtn.addEventListener('click',function(){setOD(!st.od);});
  var rulerEl=document.getElementById('a11y-ruler-line'),rulerBtn=document.getElementById('a11y-ruler-btn');
  function setRuler(on){st.ruler=on;document.body.classList.toggle('ruler-on',on);if(rulerBtn){rulerBtn.textContent=on?'On':'Off';rulerBtn.classList.toggle('on',on);}save();}
  document.addEventListener('mousemove',function(e){if(st.ruler&&rulerEl)rulerEl.style.top=(e.clientY-17)+'px';});
  if(rulerBtn)rulerBtn.addEventListener('click',function(){setRuler(!st.ruler);});
  var SZ=[1,1.18,1.36],szBtns=[document.getElementById('a11y-sz1'),document.getElementById('a11y-sz2'),document.getElementById('a11y-sz3')];
  function setSz(i){st.sz=i;document.documentElement.style.fontSize=(SZ[i-1]*16)+'px';szBtns.forEach(function(b,idx){if(b)b.classList.toggle('on',idx+1===i);});save();}
  szBtns.forEach(function(b,i){if(b)b.addEventListener('click',function(){setSz(i+1);});});
  var LS=[1.7,2.1,2.6],lsBtns=[document.getElementById('a11y-ls1'),document.getElementById('a11y-ls2'),document.getElementById('a11y-ls3')];
  function setLs(i){st.ls=i;document.body.style.lineHeight=LS[i-1];lsBtns.forEach(function(b,idx){if(b)b.classList.toggle('on',idx+1===i);});save();}
  lsBtns.forEach(function(b,i){if(b)b.addEventListener('click',function(){setLs(i+1);});});
  function save(){try{localStorage.setItem(S,JSON.stringify({bionic:st.bionic,overlay:st.overlay,od:st.od,ruler:st.ruler,sz:st.sz,ls:st.ls}));}catch(e){}}
  setOvl(st.overlay);if(st.bionic)setBio(true);setOD(!!st.od);setRuler(!!st.ruler);setSz(st.sz||1);setLs(st.ls||1);
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
</script>
<script>
// ============================================================
// QUIZ DATA — 20 questions per topic, GCSE level, mixed MCQ + T/F
// ============================================================
const quizData = [

  // TOPIC 0: NUMBER & ALGEBRA
  [
    { q:"Simplify: 2³ × 2⁴", type:"mcq", options:["2⁷","2¹²","4⁷","8¹²"], answer:0, explain:"Add the indices: 3+4=7, so 2⁷." },
    { q:"x⁰ = 1 for any non-zero x.", type:"tf", answer:true, explain:"Anything to the power 0 is 1 (except 0⁰)." },
    { q:"√50 simplifies to 5√2.", type:"tf", answer:true, explain:"√50=√(25×2)=5√2 ✓" },
    { q:"Expand: (x+3)(x−5)", type:"mcq", options:["x²−2x−15","x²+2x−15","x²−8x−15","x²−2x+15"], answer:0, explain:"x²−5x+3x−15=x²−2x−15." },
    { q:"Factorise x²−9", type:"mcq", options:["(x−3)²","(x+9)(x−1)","(x+3)(x−3)","(x−9)(x+1)"], answer:2, explain:"Difference of two squares: x²−3²=(x+3)(x−3)." },
    { q:"Solve: 4x − 7 = 21", type:"mcq", options:["x=6","x=7","x=14","x=28"], answer:1, explain:"4x=28. x=7." },
    { q:"3⁻² = 1/9.", type:"tf", answer:true, explain:"Negative index means reciprocal: 1/3²=1/9 ✓" },
    { q:"Solve simultaneously: x+y=10, x−y=4. x = ?", type:"mcq", options:["3","5","6","7"], answer:3, explain:"Adding: 2x=14, x=7." },
    { q:"(2x³)² = 4x⁶", type:"tf", answer:true, explain:"Square each part: 2²=4, (x³)²=x⁶ ✓" },
    { q:"Factorise 2x²+5x+3", type:"mcq", options:["(2x+3)(x+1)","(2x+1)(x+3)","(x+1)(2x+3)","(2x+5)(x+3)"], answer:0, explain:"(2x+3)(x+1)=2x²+2x+3x+3=2x²+5x+3 ✓" },
    { q:"Solve the inequality 2x+3<11.", type:"mcq", options:["x<4","x<7","x<8","x<5.5"], answer:0, explain:"2x<8. x<4." },
    { q:"A surd is an irrational root such as √2.", type:"tf", answer:true, explain:"Surds cannot be simplified to a whole number ✓" },
    { q:"Make x the subject of y = 3x + 2.", type:"mcq", options:["x=(y−2)/3","x=(y+2)/3","x=3(y−2)","x=y/3−2"], answer:0, explain:"y−2=3x, so x=(y−2)/3." },
    { q:"5! (5 factorial) = 120.", type:"tf", answer:true, explain:"5×4×3×2×1=120 ✓" },
    { q:"Solve x² = 49.", type:"mcq", options:["x=7 only","x=−7 only","x=±7","x=24.5"], answer:2, explain:"Both 7² and (−7)² equal 49." },
    { q:"Standard form for 0.00056 is 5.6×10⁻⁴.", type:"tf", answer:true, explain:"Move decimal point 4 places right: 5.6×10⁻⁴ ✓" },
    { q:"Solve by completing the square: x²+6x+5=0 has roots x=−1 and x=−5.", type:"tf", answer:true, explain:"(x+1)(x+5)=0 → x=−1 or x=−5 ✓" },
    { q:"Expand and simplify: 3(2x−1)−2(x+4)", type:"mcq", options:["4x−11","4x+11","8x−11","4x−7"], answer:0, explain:"6x−3−2x−8=4x−11." },
    { q:"The product of a number and its reciprocal is always 1.", type:"tf", answer:true, explain:"x × 1/x = 1 (for x≠0) ✓" },
    { q:"Solve: x²−5x+6=0", type:"mcq", options:["x=2 or 3","x=−2 or −3","x=1 or 6","x=2 or 6"], answer:0, explain:"(x−2)(x−3)=0 → x=2 or x=3." }
  ],

  // TOPIC 1: RATIO, PROPORTION & RATES
  [
    { q:"Share £200 in the ratio 3:2.", type:"mcq", options:["£120 and £80","£100 and £100","£150 and £50","£140 and £60"], answer:0, explain:"5 parts, 1 part=£40. 3×40=£120, 2×40=£80." },
    { q:"A car travels 150 miles in 2.5 hours. Its average speed is 60 mph.", type:"tf", answer:true, explain:"150÷2.5=60 mph ✓" },
    { q:"Density = mass ÷ volume.", type:"tf", answer:true, explain:"Standard compound measure formula ✓" },
    { q:"A price of £80 increases by 15%. The new price is £92.", type:"mcq", options:["£88","£92","£95","£90"], answer:1, explain:"£80×1.15=£92." },
    { q:"Compound interest on £1000 at 5% for 2 years gives £1102.50.", type:"tf", answer:true, explain:"1000×1.05²=1102.50 ✓" },
    { q:"y is inversely proportional to x. If y=4 when x=3, find y when x=6.", type:"mcq", options:["1","2","8","12"], answer:1, explain:"k=xy=12. y=12÷6=2." },
    { q:"A map scale of 1:50000 means 2 cm on the map represents 1 km in real life.", type:"tf", answer:true, explain:"2cm×50000=100000cm=1km ✓" },
    { q:"Decrease £450 by 12%.", type:"mcq", options:["£396","£408","£402","£392"], answer:0, explain:"£450×0.88=£396." },
    { q:"Reverse percentage: a jacket costs £68 after a 15% discount. The original price was £80.", type:"tf", answer:true, explain:"£68÷0.85=£80 ✓" },
    { q:"Direct proportion graphs are straight lines through the origin.", type:"tf", answer:true, explain:"y=kx passes through (0,0) ✓" },
    { q:"A recipe for 4 people uses 320g of flour. How much for 7 people?", type:"mcq", options:["480g","560g","600g","640g"], answer:1, explain:"80g per person × 7 = 560g." },
    { q:"Two quantities in inverse proportion: as one doubles, the other halves.", type:"tf", answer:true, explain:"y=k/x ✓" },
    { q:"A van uses fuel at 8 miles per litre. How many litres for a 200-mile journey?", type:"mcq", options:["20","25","30","16"], answer:1, explain:"200÷8=25 litres." },
    { q:"Simple interest on £500 at 4% per year for 3 years is £60.", type:"tf", answer:true, explain:"500×0.04×3=60 ✓" },
    { q:"Express 45 minutes as a fraction of 3 hours in simplest form.", type:"mcq", options:["1/3","1/4","3/4","1/6"], answer:1, explain:"45/180=1/4." },
    { q:"Population growth of 2% per year is an example of exponential growth.", type:"tf", answer:true, explain:"Repeated percentage multiplier = exponential ✓" },
    { q:"Share £540 in the ratio 4:5:1, the largest share is £270.", type:"tf", answer:true, explain:"10 parts, 1 part=£54. Largest=5×54=£270 ✓" },
    { q:"Best buy: 3kg for £4.50 vs 5kg for £7. Which is better value per kg?", type:"mcq", options:["3kg pack (£1.50/kg)","5kg pack (£1.40/kg)","Same value","Cannot tell"], answer:1, explain:"4.50/3=£1.50, 7/5=£1.40 — the 5kg pack is cheaper per kg." },
    { q:"A car depreciates by 10% each year. After 2 years a £20,000 car is worth £16,200.", type:"tf", answer:true, explain:"20000×0.9²=16200 ✓" },
    { q:"Pressure = Force ÷ Area is a compound measure.", type:"tf", answer:true, explain:"Combines two quantities into a rate ✓" }
  ],

  // TOPIC 2: GEOMETRY & MEASURES
  [
    { q:"The angle in a semicircle is always 90°.", type:"tf", answer:true, explain:"Circle theorem: angle in a semicircle = 90° ✓" },
    { q:"Pythagoras' theorem: a²+b²=c², where c is the hypotenuse.", type:"tf", answer:true, explain:"True for right-angled triangles ✓" },
    { q:"A right-angled triangle has legs 6cm and 8cm. The hypotenuse is:", type:"mcq", options:["10cm","12cm","14cm","9cm"], answer:0, explain:"√(6²+8²)=√100=10cm." },
    { q:"In a right triangle, tan(θ) = opposite/adjacent.", type:"tf", answer:true, explain:"SOH CAH TOA — tan = opp/adj ✓" },
    { q:"Find x: sin(x)=0.5. x ≈ ?", type:"mcq", options:["30°","45°","60°","90°"], answer:0, explain:"sin(30°)=0.5." },
    { q:"The angle between a tangent and a radius at the point of contact is 90°.", type:"tf", answer:true, explain:"Tangent–radius circle theorem ✓" },
    { q:"Volume of a cylinder = πr²h.", type:"tf", answer:true, explain:"Standard formula ✓" },
    { q:"A cylinder has radius 3cm and height 10cm. Its volume is approximately:", type:"mcq", options:["94.2 cm³","188.4 cm³","282.6 cm³","30 cm³"], answer:1, explain:"π×3²×10≈188.4 cm³." },
    { q:"Similar shapes have equal angles and proportional sides.", type:"tf", answer:true, explain:"Definition of similarity ✓" },
    { q:"If two shapes are similar with scale factor 2, their areas are in the ratio:", type:"mcq", options:["1:2","1:4","2:1","1:8"], answer:1, explain:"Area scale factor = (length scale factor)² = 4." },
    { q:"Volume scale factor equals (length scale factor)³.", type:"tf", answer:true, explain:"Volumes scale with the cube of the linear scale factor ✓" },
    { q:"The angle subtended by an arc at the centre is twice the angle at the circumference.", type:"tf", answer:true, explain:"Circle theorem ✓" },
    { q:"A cone has radius 4cm and slant height 5cm. Its curved surface area (πrl) is:", type:"mcq", options:["20π cm²","16π cm²","9π cm²","40π cm²"], answer:0, explain:"π×4×5=20π cm²." },
    { q:"Vectors can be used to describe translations.", type:"tf", answer:true, explain:"A column vector gives horizontal and vertical movement ✓" },
    { q:"Find the bearing of B from A if A is due south of B.", type:"mcq", options:["000°","090°","180°","270°"], answer:0, explain:"B is due north of A, so bearing of B from A is 000°." },
    { q:"Cyclic quadrilaterals have opposite angles that sum to 180°.", type:"tf", answer:true, explain:"Circle theorem for cyclic quadrilaterals ✓" },
    { q:"Arc length = (θ/360) × 2πr.", type:"tf", answer:true, explain:"Arc length formula ✓" },
    { q:"A ladder 5m long leans against a wall, its base 3m from the wall. How high up the wall does it reach?", type:"mcq", options:["3m","4m","5m","6m"], answer:1, explain:"√(5²−3²)=√16=4m." },
    { q:"Congruent shapes are identical in shape and size.", type:"tf", answer:true, explain:"Congruent = same shape AND size ✓" },
    { q:"The cosine rule can be used for any triangle, not just right-angled ones.", type:"tf", answer:true, explain:"Cosine and sine rules apply to all triangles ✓" }
  ],

  // TOPIC 3: STATISTICS & PROBABILITY
  [
    { q:"The interquartile range is Q3 − Q1.", type:"tf", answer:true, explain:"IQR = upper quartile − lower quartile ✓" },
    { q:"A box plot shows the minimum, Q1, median, Q3 and maximum.", type:"tf", answer:true, explain:"Five-number summary ✓" },
    { q:"Cumulative frequency graphs are used to estimate the median and quartiles.", type:"tf", answer:true, explain:"Read across at the relevant cumulative frequency ✓" },
    { q:"A tree diagram shows two events. P(A then B) for independent events =", type:"mcq", options:["P(A)+P(B)","P(A)×P(B)","P(A)−P(B)","P(A)/P(B)"], answer:1, explain:"Multiply along the branches for independent events." },
    { q:"For dependent events, the second probability changes based on the first outcome.", type:"tf", answer:true, explain:"E.g. picking without replacement ✓" },
    { q:"A bag has 4 red and 6 blue balls. P(red then blue, without replacement) =", type:"mcq", options:["24/100","24/90","4/10","6/10"], answer:1, explain:"4/10 × 6/9 = 24/90." },
    { q:"Histograms use frequency density on the y-axis.", type:"tf", answer:true, explain:"Frequency density = frequency ÷ class width ✓" },
    { q:"Frequency density = frequency × class width.", type:"tf", answer:false, explain:"It's frequency ÷ class width, not multiplied." },
    { q:"A scatter graph showing height vs. shoe size would likely show:", type:"mcq", options:["No correlation","Positive correlation","Negative correlation","Perfect correlation"], answer:1, explain:"Taller people tend to have larger feet — positive correlation." },
    { q:"The mean is found by dividing the sum of values by the number of values.", type:"tf", answer:true, explain:"Standard definition of the mean ✓" },
    { q:"Estimate the mean from a grouped frequency table using the midpoint of each class.", type:"tf", answer:true, explain:"Midpoints represent each group for an estimated mean ✓" },
    { q:"Two events are mutually exclusive if P(A and B) = 0.", type:"tf", answer:true, explain:"They cannot happen at the same time ✓" },
    { q:"Sample space for two coins flipped: how many outcomes?", type:"mcq", options:["2","3","4","8"], answer:2, explain:"HH, HT, TH, TT = 4 outcomes." },
    { q:"A box plot with a long whisker on the right suggests the data is positively skewed.", type:"tf", answer:true, explain:"Skew is towards the longer tail ✓" },
    { q:"Stratified sampling ensures groups are represented proportionally.", type:"tf", answer:true, explain:"Sample size from each group reflects its proportion in the population ✓" },
    { q:"P(at least one head in two coin flips) =", type:"mcq", options:["1/4","1/2","3/4","1"], answer:2, explain:"P(no heads)=1/4, so P(at least one)=1−1/4=3/4." },
    { q:"The range is the most reliable measure of spread because it uses all the data.", type:"tf", answer:false, explain:"The range is affected heavily by outliers — IQR is more reliable." },
    { q:"In a box plot, the line inside the box represents the median.", type:"tf", answer:true, explain:"Median splits the box into Q1–median and median–Q3 ✓" },
    { q:"Relative frequency approaches theoretical probability as the number of trials increases.", type:"tf", answer:true, explain:"Law of large numbers ✓" },
    { q:"A pie chart with a 120° sector represents what fraction of the total?", type:"mcq", options:["1/2","1/3","1/4","1/6"], answer:1, explain:"120/360=1/3." }
  ],

  // TOPIC 4: GRAPHS & FUNCTIONS
  [
    { q:"The graph of y=x² is a parabola.", type:"tf", answer:true, explain:"Quadratic graphs are U-shaped (or ∩-shaped) parabolas ✓" },
    { q:"The gradient of the line y=4x−3 is:", type:"mcq", options:["4","−3","3","−4"], answer:0, explain:"In y=mx+c, m is the gradient: m=4." },
    { q:"Parallel lines have the same gradient.", type:"tf", answer:true, explain:"Same gradient = lines never meet ✓" },
    { q:"Perpendicular lines have gradients that multiply to give:", type:"mcq", options:["0","1","−1","2"], answer:2, explain:"Gradients of perpendicular lines multiply to −1." },
    { q:"The line y=2x+5 crosses the y-axis at (0,5).", type:"tf", answer:true, explain:"y-intercept is the constant term, c=5 ✓" },
    { q:"y=x³ is an example of a cubic graph.", type:"mcq", options:["Linear","Quadratic","Cubic","Reciprocal"], answer:2, explain:"Highest power of x is 3 — cubic." },
    { q:"The graph of y=1/x is called a reciprocal graph and never touches the axes.", type:"tf", answer:true, explain:"Asymptotes at x=0 and y=0 ✓" },
    { q:"To find where two graphs intersect, you can solve their equations simultaneously.", type:"tf", answer:true, explain:"Intersection points satisfy both equations ✓" },
    { q:"The graph y=(x−2)² is a translation of y=x² by:", type:"mcq", options:["2 units left","2 units right","2 units up","2 units down"], answer:1, explain:"y=(x−a)² shifts the graph 'a' units to the right." },
    { q:"The graph y=x²+3 is a translation of y=x² by 3 units up.", type:"tf", answer:true, explain:"Adding a constant shifts the graph vertically ✓" },
    { q:"A quadratic graph y=x²−4 crosses the x-axis when x=", type:"mcq", options:["x=±2","x=±4","x=2 only","x=−4 only"], answer:0, explain:"x²=4, so x=±2." },
    { q:"The midpoint of (2,4) and (6,10) is (4,7).", type:"tf", answer:true, explain:"Average the coordinates: ((2+6)/2,(4+10)/2)=(4,7) ✓" },
    { q:"Distance between (0,0) and (3,4) using Pythagoras is:", type:"mcq", options:["5","7","6","12"], answer:0, explain:"√(3²+4²)=√25=5." },
    { q:"y=−x² produces an n-shaped (downward) parabola.", type:"tf", answer:true, explain:"Negative coefficient flips the parabola ✓" },
    { q:"A speed-time graph's gradient represents acceleration.", type:"tf", answer:true, explain:"Rate of change of speed = acceleration ✓" },
    { q:"The area under a speed-time graph represents:", type:"mcq", options:["Acceleration","Distance","Velocity","Time"], answer:1, explain:"Area = speed × time = distance." },
    { q:"The line x=3 is a vertical line.", type:"tf", answer:true, explain:"x is constant for all y values — a vertical line ✓" },
    { q:"To solve x²−5x+6=0 graphically, find where y=x²−5x+6 crosses the x-axis.", type:"tf", answer:true, explain:"Roots of the equation = x-intercepts of the graph ✓" },
    { q:"The equation of a circle centred at the origin with radius 5 is:", type:"mcq", options:["x+y=5","x²+y²=5","x²+y²=25","x²−y²=25"], answer:2, explain:"x²+y²=r² where r=5, so x²+y²=25." },
    { q:"An exponential graph y=2ˣ increases more slowly as x increases.", type:"tf", answer:false, explain:"Exponential growth gets faster, not slower, as x increases." }
  ]
];

// ============================================================
// TEST DATA — 50 questions (10 per topic, mixed MCQ + T/F)
// ============================================================
const testData = [
  // NUMBER & ALGEBRA (10)
  { q:"x⁵ ÷ x² = x³.", type:"tf", answer:true, explain:"Subtract indices: 5−2=3 ✓" },
  { q:"Expand (x−4)(x+4) = x²−16.", type:"tf", answer:true, explain:"Difference of two squares ✓" },
  { q:"Solve 5x+2=27: x=5.", type:"tf", answer:true, explain:"5x=25, x=5 ✓" },
  { q:"Factorise x²+7x+12 =", type:"mcq", options:["(x+3)(x+4)","(x+2)(x+6)","(x+1)(x+12)","(x−3)(x−4)"], answer:0, explain:"3×4=12, 3+4=7." },
  { q:"√72 simplifies to 6√2.", type:"tf", answer:true, explain:"√72=√(36×2)=6√2 ✓" },
  { q:"4⁻¹ = 1/4.", type:"tf", answer:true, explain:"Negative power = reciprocal ✓" },
  { q:"Solve x²−16=0: x = ?", type:"mcq", options:["x=4 only","x=±4","x=−4 only","x=8"], answer:1, explain:"x²=16, x=±4." },
  { q:"Make x the subject of v=u+at: x=(v−u)/a (where x=t).", type:"tf", answer:true, explain:"Rearranging: t=(v−u)/a ✓" },
  { q:"Simplify 3x+2y−x+5y = 2x+7y.", type:"tf", answer:true, explain:"(3x−x)+(2y+5y)=2x+7y ✓" },
  { q:"Solve simultaneously: 2x+y=11, x−y=1. x=4.", type:"tf", answer:true, explain:"Adding: 3x=12, x=4 ✓" },
  // RATIO, PROPORTION & RATES (10)
  { q:"Share £90 in ratio 1:2:3 gives £15, £30, £45.", type:"tf", answer:true, explain:"6 parts, 1 part=£15 ✓" },
  { q:"A car travels 240km in 4 hours: average speed = 60km/h.", type:"tf", answer:true, explain:"240÷4=60 ✓" },
  { q:"Increase £150 by 8% gives £162.", type:"tf", answer:true, explain:"150×1.08=162 ✓" },
  { q:"Reverse percentage: £176 after a 12% decrease — original price was £200.", type:"tf", answer:true, explain:"176÷0.88=200 ✓" },
  { q:"Compound interest of £2000 at 3% for 2 years gives approximately £2121.80.", type:"tf", answer:true, explain:"2000×1.03²≈2121.80 ✓" },
  { q:"y∝1/x, y=5 when x=2. Find y when x=10.", type:"mcq", options:["1","2","25","0.5"], answer:0, explain:"k=10. y=10÷10=1." },
  { q:"Map scale 1:25000: 4cm represents 1km.", type:"tf", answer:true, explain:"4×25000=100000cm=1km ✓" },
  { q:"Density formula: density = mass × volume.", type:"tf", answer:false, explain:"Density = mass ÷ volume, not multiplied." },
  { q:"5kg for £6 vs 8kg for £10: the 8kg pack is better value per kg.", type:"tf", answer:false, explain:"6/5=£1.20/kg, 10/8=£1.25/kg — the 5kg pack is actually better value." },
  { q:"A van uses fuel at 10 miles/litre. Litres needed for 250 miles = 25.", type:"tf", answer:true, explain:"250÷10=25 ✓" },
  // GEOMETRY & MEASURES (10)
  { q:"Pythagoras: legs 5cm and 12cm give a hypotenuse of 13cm.", type:"tf", answer:true, explain:"√(25+144)=√169=13 ✓" },
  { q:"The angle in a semicircle is 90°.", type:"tf", answer:true, explain:"Circle theorem ✓" },
  { q:"Volume of a cylinder r=2cm, h=5cm ≈ 62.8 cm³.", type:"tf", answer:true, explain:"π×4×5≈62.8 ✓" },
  { q:"Similar shapes with scale factor 3 have areas in ratio:", type:"mcq", options:["1:3","1:6","1:9","1:27"], answer:2, explain:"Area ratio = scale factor² = 9." },
  { q:"sin(90°)=1.", type:"tf", answer:true, explain:"Standard trig value ✓" },
  { q:"Cyclic quadrilateral opposite angles sum to 180°.", type:"tf", answer:true, explain:"Circle theorem ✓" },
  { q:"Bearings are always measured clockwise from north.", type:"tf", answer:true, explain:"Standard bearing convention ✓" },
  { q:"Volume scale factor = (length scale factor)².", type:"tf", answer:false, explain:"Volume scale factor = (length scale factor)³, not squared." },
  { q:"A right triangle with opposite=3, adjacent=4: tan(θ)=3/4.", type:"tf", answer:true, explain:"tan=opp/adj=3/4 ✓" },
  { q:"Arc length formula uses (θ/360)×2πr.", type:"tf", answer:true, explain:"Standard arc length formula ✓" },
  // STATISTICS & PROBABILITY (10)
  { q:"IQR = Q3 − Q1.", type:"tf", answer:true, explain:"Definition of interquartile range ✓" },
  { q:"For independent events, P(A and B)=P(A)×P(B).", type:"tf", answer:true, explain:"Multiplication rule ✓" },
  { q:"Bag has 5 red, 3 blue. P(red then red, no replacement) = 20/56.", type:"tf", answer:true, explain:"5/8 × 4/7 = 20/56 ✓" },
  { q:"Histograms plot frequency density, not frequency, on the y-axis.", type:"tf", answer:true, explain:"Frequency density = frequency ÷ class width ✓" },
  { q:"Cumulative frequency graphs estimate the median at 50% of the total frequency.", type:"tf", answer:true, explain:"Median = middle value, at the 50th percentile ✓" },
  { q:"A scatter graph with points scattered randomly shows strong correlation.", type:"tf", answer:false, explain:"Random scatter = no correlation." },
  { q:"Two coins flipped: P(exactly one head) = 1/2.", type:"mcq", options:["1/4","1/2","3/4","1"], answer:1, explain:"HT and TH out of 4 outcomes = 2/4=1/2." },
  { q:"Stratified sampling keeps group proportions consistent with the population.", type:"tf", answer:true, explain:"Correct definition ✓" },
  { q:"The mean uses every value in the data set.", type:"tf", answer:true, explain:"Mean = sum of all values ÷ number of values ✓" },
  { q:"A pie chart sector of 90° out of 360° represents 1/4 of the data.", type:"tf", answer:true, explain:"90/360=1/4 ✓" },
  // GRAPHS & FUNCTIONS (10)
  { q:"y=mx+c: m is the gradient and c is the y-intercept.", type:"tf", answer:true, explain:"Standard linear equation form ✓" },
  { q:"Parallel lines have equal gradients.", type:"tf", answer:true, explain:"Same steepness, never intersect ✓" },
  { q:"Perpendicular gradients multiply to give −1.", type:"tf", answer:true, explain:"m1×m2=−1 ✓" },
  { q:"y=x² is a cubic graph.", type:"tf", answer:false, explain:"y=x² is quadratic; y=x³ is cubic." },
  { q:"The graph y=(x+2)² is a translation of y=x² by 2 units left.", type:"tf", answer:true, explain:"y=(x−a)² shifts right by a; here a=−2, so shift left by 2 ✓" },
  { q:"The area under a speed-time graph represents distance.", type:"tf", answer:true, explain:"Speed × time = distance ✓" },
  { q:"The midpoint of (1,1) and (5,7) is (3,4).", type:"tf", answer:true, explain:"((1+5)/2,(1+7)/2)=(3,4) ✓" },
  { q:"Distance between (0,0) and (6,8) is:", type:"mcq", options:["10","14","8","12"], answer:0, explain:"√(36+64)=√100=10." },
  { q:"x²+y²=49 represents a circle of radius 49.", type:"tf", answer:false, explain:"Radius = √49 = 7, not 49." },
  { q:"y=2ˣ is an exponential graph showing growth.", type:"tf", answer:true, explain:"Exponential graphs grow increasingly fast ✓" }
];

// ============================================================
// STATE
// ============================================================
let currentTopicIdx = 0;
let currentQuestions = [];
let currentQIdx = 0;
let quizAnswered = false;
let quizResults = [];
let testAnswers = {};
let testSubmitted = false;

const topicMeta = [
  { title:"🔢 Number & Algebra", desc:"Indices, surds, equations, expanding & factorising" },
  { title:"⚖️ Ratio, Proportion & Rates", desc:"Ratio, percentages, compound measures, growth & decay" },
  { title:"📐 Geometry & Measures", desc:"Angles, circle theorems, Pythagoras, trigonometry" },
  { title:"📊 Statistics & Probability", desc:"Averages, cumulative frequency, probability trees" },
  { title:"📈 Graphs & Functions", desc:"Linear, quadratic graphs, gradients & transformations" }
];

// ============================================================
// NAVIGATION
// ============================================================
function showScreen(id) {
  document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));
  document.getElementById(id).classList.add('active');
}

function showHome() {
  showScreen('screenHome');
  document.querySelectorAll('.topic-btn').forEach(b=>b.classList.remove('active'));
}

// ============================================================
// QUIZ
// ============================================================
function startQuiz(topicIdx) {
  currentTopicIdx = topicIdx;
  currentQuestions = [...quizData[topicIdx]].sort(()=>Math.random()-0.5);
  currentQIdx = 0;
  quizAnswered = false;
  quizResults = [];

  document.getElementById('quizTopicTitle').textContent = topicMeta[topicIdx].title;
  document.getElementById('quizTopicDesc').textContent = topicMeta[topicIdx].desc;

  document.querySelectorAll('.topic-btn').forEach(b=>b.classList.remove('active'));
  const btn = document.getElementById('topicBtn'+topicIdx);
  if (btn) btn.classList.add('active');

  showScreen('screenQuiz');
  renderQuestion();
}

function renderQuestion() {
  const total = currentQuestions.length;
  const q = currentQuestions[currentQIdx];
  quizAnswered = false;

  document.getElementById('questionCounter').textContent = 'Question '+(currentQIdx+1)+' of '+total;
  document.getElementById('questionText').textContent = q.q;
  document.getElementById('quizProgressFill').style.width = ((currentQIdx/total)*100)+'%';

  const fb = document.getElementById('feedbackBox');
  fb.className = 'feedback-box';
  fb.innerHTML = '';
  document.getElementById('nextBtn').style.display = 'none';

  const area = document.getElementById('answerArea');
  area.innerHTML = '';

  if (q.type === 'mcq') {
    const opts = document.createElement('div');
    opts.className = 'options';
    q.options.forEach((opt, i) => {
      const btn = document.createElement('button');
      btn.className = 'option-btn';
      btn.innerHTML = '<span class="opt-letter">'+String.fromCharCode(65+i)+'</span>'+opt;
      btn.onclick = () => answerMcq(i, q);
      opts.appendChild(btn);
    });
    area.appendChild(opts);
  } else {
    const row = document.createElement('div');
    row.className = 'tf-row';
    const tBtn = document.createElement('button');
    tBtn.className = 'tf-btn true-btn';
    tBtn.textContent = '✓ True';
    tBtn.onclick = () => answerTf(true, q);
    const fBtn = document.createElement('button');
    fBtn.className = 'tf-btn false-btn';
    fBtn.textContent = '✗ False';
    fBtn.onclick = () => answerTf(false, q);
    row.appendChild(tBtn); row.appendChild(fBtn);
    area.appendChild(row);
  }
}

function answerMcq(selected, q) {
  if (quizAnswered) return;
  quizAnswered = true;
  const correct = selected === q.answer;
  quizResults.push({ q:q.q, correct, yourAnswer:q.options[selected], modelAnswer:q.options[q.answer] });

  document.querySelectorAll('.option-btn').forEach((b,i) => {
    b.disabled = true;
    if (i === q.answer) b.classList.add('correct-ans');
    else if (i === selected && !correct) b.classList.add('wrong-ans');
  });
  showFeedback(correct, q.explain);
}

function answerTf(selected, q) {
  if (quizAnswered) return;
  quizAnswered = true;
  const correct = selected === q.answer;
  quizResults.push({ q:q.q, correct, yourAnswer:selected?'True':'False', modelAnswer:q.answer?'True':'False' });

  document.querySelectorAll('.tf-btn').forEach(b => {
    b.disabled = true;
    const isTrueBtn = b.classList.contains('true-btn');
    if (isTrueBtn === q.answer) b.classList.add('correct-ans');
    else if (isTrueBtn === selected && !correct) b.classList.add('wrong-ans');
  });
  showFeedback(correct, q.explain);
}

function showFeedback(correct, explain) {
  const fb = document.getElementById('feedbackBox');
  fb.className = 'feedback-box show '+(correct?'correct':'incorrect');
  fb.innerHTML = '<div class="fb-title">'+(correct?'✅ Correct!':'❌ Incorrect')+'</div><div class="fb-explain">'+explain+'</div>';
  document.getElementById('nextBtn').style.display = '';
}

function nextQuestion() {
  currentQIdx++;
  if (currentQIdx >= currentQuestions.length) showQuizResult();
  else renderQuestion();
}

function showQuizResult() {
  const total = quizResults.length;
  const correct = quizResults.filter(r=>r.correct).length;
  const pct = Math.round(correct/total*100);

  document.getElementById('resultScore').textContent = pct+'%';
  document.getElementById('resultLabel').textContent = correct+' out of '+total+' correct';
  document.getElementById('resultEmoji').textContent = pct>=80?'🎉':pct>=60?'😊':'😬';
  document.getElementById('resultMessage').textContent = pct>=80?'Brilliant work, Aruna!':pct>=60?'Good effort — keep at it!':'More revision needed!';

  document.getElementById('quiz-retry-nudge').style.display = pct<80?'':'none';
  document.getElementById('quiz-pass-actions').style.display = pct>=80?'':'none';
  document.getElementById('reviewSection').style.display = 'none';
  showScreen('screenResult');
}

function retryQuiz() { startQuiz(currentTopicIdx); }

function showReview() {
  const section = document.getElementById('reviewSection');
  section.style.display = '';
  const list = document.getElementById('reviewList');
  list.innerHTML = '';
  quizResults.forEach(r => {
    const li = document.createElement('li');
    li.className = 'review-item '+(r.correct?'correct':'incorrect');
    li.innerHTML = `<div class="review-q">${r.correct?'✅':'❌'} ${r.q}</div>
      <div class="review-answer">
        <div class="your">Your answer: <span>${r.yourAnswer}</span></div>
        <div class="model">Correct: <span>${r.modelAnswer}</span></div>
      </div>`;
    list.appendChild(li);
  });
  section.scrollIntoView({ behavior:'smooth' });
}

// ============================================================
// TEST
// ============================================================
function startTest() {
  testAnswers = {};
  testSubmitted = false;

  document.querySelectorAll('.topic-btn').forEach(b=>b.classList.remove('active'));
  const btn = document.getElementById('topicBtnTest');
  if (btn) btn.classList.add('active');

  document.getElementById('submitBtn').disabled = false;
  document.getElementById('testReviewSection').style.display = 'none';

  const body = document.getElementById('test-body');
  body.innerHTML = '';

  testData.forEach((q, i) => {
    const block = document.createElement('div');
    block.className = 'test-question-block';

    let inputHtml = '';
    if (q.type === 'mcq') {
      inputHtml = '<div class="test-options">'+q.options.map((opt,j)=>
        `<div class="test-option" id="to_${i}_${j}" onclick="selectTestOption(${i},${j})"><span class="test-opt-letter">${String.fromCharCode(65+j)}</span>${opt}</div>`
      ).join('')+'</div>';
    } else {
      inputHtml = `<div class="test-tf-row">
        <button class="test-tf-btn" id="ttf_${i}_true" onclick="selectTestTf(${i},true)">✓ True</button>
        <button class="test-tf-btn" id="ttf_${i}_false" onclick="selectTestTf(${i},false)">✗ False</button>
      </div>`;
    }

    block.innerHTML = `<div class="test-q-num">Question ${i+1} of 50</div>
      <div class="test-q-text">${q.q}</div>${inputHtml}`;
    body.appendChild(block);
  });

  showScreen('screenTest');
}

function selectTestOption(qIdx, optIdx) {
  if (testSubmitted) return;
  testAnswers[qIdx] = optIdx;
  document.querySelectorAll(`[id^="to_${qIdx}_"]`).forEach((el,i)=>el.classList.toggle('selected', i===optIdx));
}

function selectTestTf(qIdx, value) {
  if (testSubmitted) return;
  testAnswers[qIdx] = value;
  document.getElementById(`ttf_${qIdx}_true`).classList.toggle('selected', value===true);
  document.getElementById(`ttf_${qIdx}_false`).classList.toggle('selected', value===false);
}

function submitTest() {
  const unanswered = testData.filter((_,i)=>testAnswers[i]===undefined).length;
  if (unanswered > 0 && !confirm(`You have ${unanswered} unanswered question(s). Submit anyway?`)) return;

  testSubmitted = true;
  document.getElementById('submitBtn').disabled = true;

  let correct = 0;
  testData.forEach((q,i) => {
    if (testAnswers[i] === undefined) return;
    const isCorrect = testAnswers[i] === q.answer;
    if (isCorrect) correct++;

    if (q.type === 'mcq') {
      document.querySelectorAll(`[id^="to_${i}_"]`).forEach((el,j) => {
        el.classList.add('disabled'); el.classList.remove('selected');
        if (j===q.answer) el.classList.add('correct-ans');
        else if (j===testAnswers[i] && !isCorrect) el.classList.add('wrong-ans');
      });
    } else {
      const tBtn = document.getElementById(`ttf_${i}_true`);
      const fBtn = document.getElementById(`ttf_${i}_false`);
      tBtn.classList.remove('selected'); fBtn.classList.remove('selected');
      (q.answer?tBtn:fBtn).classList.add('correct-ans');
      if (testAnswers[i]!==undefined && testAnswers[i]!==q.answer)
        (testAnswers[i]?tBtn:fBtn).classList.add('wrong-ans');
    }
  });

  const pct = Math.round(correct/50*100);
  const answered = Object.keys(testAnswers).length;
  document.getElementById('testResultScore').textContent = pct+'%';
  document.getElementById('tcCorrect').textContent = correct;
  document.getElementById('tcIncorrect').textContent = answered-correct;
  document.getElementById('tcPercent').textContent = pct+'%';
  document.getElementById('testResultEmoji').textContent = pct>=80?'🏆':pct>=60?'📚':'😬';
  document.getElementById('testResultMsg').textContent = pct>=80?'Brilliant — you passed the test, Aruna!':pct>=60?'Good effort — nearly there!':'More revision needed!';
  document.getElementById('test-retry-nudge').style.display = pct<80?'':'none';
  showScreen('screenTestResult');
}

function showTestReview() {
  const section = document.getElementById('testReviewSection');
  section.style.display = '';
  const list = document.getElementById('testReviewList');
  list.innerHTML = '';
  testData.forEach((q,i) => {
    const userAns = testAnswers[i];
    if (userAns===undefined) return;
    const correct = userAns===q.answer;
    const yourTxt = q.type==='mcq'?q.options[userAns]:(userAns?'True':'False');
    const modelTxt = q.type==='mcq'?q.options[q.answer]:(q.answer?'True':'False');
    const li = document.createElement('li');
    li.className = 'review-item '+(correct?'correct':'incorrect');
    li.innerHTML = `<div class="review-q">${correct?'✅':'❌'} Q${i+1}: ${q.q}</div>
      <div class="review-answer">
        <div class="your">Your answer: <span>${yourTxt}</span></div>
        <div class="model">Correct: <span>${modelTxt}</span></div>
      </div>`;
    list.appendChild(li);
  });
  section.scrollIntoView({ behavior:'smooth' });
}
</script></body>
</html>
